<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.darwin.photolandhk">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_only"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_only"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".product_library.ProductLibraryActivity"
            android:label="@string/title_activity_product_library"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".post.PostContentActivity" />
        <activity android:name=".posts_overview.PostsOverviewActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3302105142793013~6421904163" />
    </application>

</manifest>